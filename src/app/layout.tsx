import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GEM Safety Solutions - Higiene, Seguridad Industrial y Arquitectura Cloud en Córdoba",
  description: "Consultora experta en Higiene y Seguridad, Auditorías ISO (45001, 14001, 9001), e Infraestructura Cloud AWS en Córdoba, Argentina. Seguridad integrada.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
